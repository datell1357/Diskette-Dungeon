# 05 · 기술 스택 & 빌드

## 목표

단독 Windows `.exe`, **압축 해제 후 1.44MB 이하**, 외부 의존성 0, 절차 생성 중심.

## 언어 & 라이브러리

- **언어: C (C99/C11)** 또는 절제된 C++. 작은 바이너리·완전한 제어.
- **플랫폼/렌더: [sokol](https://github.com/floooh/sokol) 헤더 라이브러리** (`sokol_app.h`, `sokol_gfx.h`, `sokol_glue.h`)
  - 헤더 온리, 매우 가벼움, OpenGL/D3D11 백엔드. 정적 링크해도 footprint 작음.
  - 대안: 순수 Win32 + OpenGL 로더(글로벌 함수 직접 로드). raylib 정적은 ~1MB라 비추(여유 줄어듦).
- **수학:** 자체 미니 벡터/행렬(헤더 1개) 또는 `HandmadeMath`.
- **외부 런타임 금지:** .NET/Mono/Electron/브라우저 일절 사용 안 함(브라우저 게임 규정 위반·용량).

## 그래픽

- OpenGL 3.3 (또는 sokol 백엔드 자동). 셰이더는 GLSL 텍스트로 임베드.
- 파이프라인 상세: [03-graphics-pipeline.md](03-graphics-pipeline.md).

## 오디오 — 절차적 신스 (wav 파일 0KB)

- 자체 소프트 신스: 펄스/삼각/노이즈 오실레이터 + ADSR + 간단 필터.
- BGM: 트래커식 패턴 데이터(작은 배열) → 런타임 합성. 칩튠 무드(어둑·서정).
- SFX: 파라미터 기반 생성(sfxr 계열 알고리즘 내장).
- 출력: sokol_audio 또는 Win32 WASAPI/XAudio2.

## 에셋 임베딩

- 모든 리소스(스프라이트·폰트·패턴)는 **바이트 배열로 exe에 임베드**(별도 파일 없음 → "단독 실행").
- 빌드 스크립트가 `assets/` → `assets_generated.c`(deflate 압축 바이트) 변환.
- 런타임에 메모리에서 해제(internal compression 허용 규정 활용).

## 절차 생성 / RNG

- 결정적 RNG(xorshift/PCG) + **시드**. 시드로 던전·적·보상 재현(데일리·공유).
- 던전: 방 그래프 생성 → 방 내부는 Wang 타일/노이즈/룸 템플릿 조합.

## 세이브

- 메타 진행만 저장: 해금·재화·최고기록·옵션·시드 기록.
- 위치: `%APPDATA%/DisketteDungeon/save.bin` (작은 바이너리, 버전 헤더 + 체크섬).
- 런 중 세이브 없음(로그라이크). 손상 시 기본값 폴백.

## 빌드

- 컴파일러: MSVC `cl` 또는 `clang-cl`. 릴리스 최적화 `/O2 /GL`(size 고려 `/O1` 비교).
- 링크: 정적, CRT 의존 최소화(가능하면 `/MT` 또는 no-CRT 미니멀).
- 산출물: `DisketteDungeon.exe` 1개.
- **용량 게이트:** 빌드 후 스크립트가 exe 크기 검사 → 1,474,560 bytes 초과 시 빌드 실패 처리.

## 권장 디렉터리 구조

```
/src
  main.c            # 엔트리, 루프, sokol 콜백
  game/             # 상태머신, 런, 방, 엔티티
  render/           # 디퍼드 라이팅, 파티클, 포스트
  audio/            # 신스, 트래커, sfx
  gen/              # 던전·적·보상 절차생성
  story/            # 추억 조각, 컷, 엔딩 분기
  shaders/          # .glsl (빌드 시 문자열 임베드)
/assets             # 원본 도트·폰트 (빌드 시 압축 임베드)
/tools
  embed.py          # assets -> C 바이트 배열
  size_gate.py      # 용량 검사
/build
```

## 품질/검증 (심사 ① 완성도, ② 용량)

- CI 또는 로컬 스크립트: 빌드 → 용량 게이트 → 스모크 실행(런 자동 진행) → 크래시 0 확인.
- 저사양/통합 그래픽 1대 이상에서 60fps 확인.
- 옵션으로 라이팅/포스트/해상도 스케일 제공(접근성 + 성능 폴백).

## 리스크 & 대응

| 리스크 | 대응 |
|---|---|
| 라이팅 성능 | 광원/파티클 상한, 근접 광원만 그림자, 품질 옵션 |
| 용량 초과 | size_gate로 조기 감지, 압축 임베드, 절차 우선 |
| 절차 애니 난이도 | 단순 본 + IK부터, 폴백으로 소수 프레임 도트 |
| 일정 | 수직 슬라이스 우선(README 우선순위), 콘텐츠는 시드/변형으로 증폭 |
